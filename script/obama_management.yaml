# Obama Management Scripts

shutdown_obama:
  sequence:
    - service: rest_command.shutdown_obama
    # - service: hassio.addon_stdin
    #   data:
    #     addon: core_rpc_shutdown
    #     input: obama
