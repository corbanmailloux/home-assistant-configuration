# Default View (Home tab)

default_view:
  view: yes
  entities:
    - updater.updater
    - device_tracker.ida_ida
    - device_tracker.jackie_jackie
    - device_tracker.ida
    - device_tracker.jackie
    - group.obama_download_display # Only shown when downloads are waiting
    - group.living_room_lights
    - group.bedroom_lights
    - media_player.living_room

# devices:
#   name: Device Trackers
#   entities:
#     - device_tracker.ida_ida
#     - device_tracker.jackie_jackie
